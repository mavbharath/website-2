/* Reset default margin and padding */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Basic styling for header and navigation */
header {
    background-color: #333;
    color: #fff;
    padding: 10px 0;
    text-align: center;
}

nav ul {
    list-style-type: none;
}

nav ul li {
    display: inline;
    margin: 0 10px;
}

nav ul li a {
    color: #fff;
    text-decoration: none;
}

/* Example styling for sections */
section {
    padding: 50px 20px;
}

section h1, section h2, section h3 {
    text-align: center;
}

/* Example styling for portfolio items */
.portfolio-item {
    text-align: center;
    margin-bottom: 20px;
}

.portfolio-item img {
    max-width: 100%;
}

/* Example styling for contact form */
form {
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
}

form label {
    display: block;
    margin-bottom: 10px;
}

form input[type="text"], form input[type="email"], form textarea {
    width: 100%;
    padding: 10px;
    margin-bottom: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
}

form button {
    background-color: #333;
    color: #fff;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
}

form button:hover {
    background-color: #555;
}
